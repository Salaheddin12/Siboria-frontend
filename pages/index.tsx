import type { NextPage } from "next";
import { Box } from "@chakra-ui/react";
import Hero from "../components/Hero";
const Home: NextPage = () => {
  return (
    <Box>
      <Hero />
    </Box>
  );
};

export default Home;
